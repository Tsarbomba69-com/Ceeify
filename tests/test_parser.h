#pragma once
#ifndef TEST_PARSER_H_
#define TEST_PARSER_H_

#include "parser.h"
#include "utils.h"
#include <unity.h>
#endif

void test_parser_single_number(void) {
  // Lexer lexer = tokenize("123");
  // Parser parser = parse(lexer);
  // TEST_ASSERT_EQUAL_INT(LITERAL, parser.ast);
  // TEST_ASSERT_EQUAL_STRING("123", ast->left->token->lexeme);
  // TEST_ASSERT_NULL(ast->left);
  // TEST_ASSERT_NULL(ast->right);
}